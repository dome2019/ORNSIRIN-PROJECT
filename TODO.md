# Ornsirin Health Link — Backlog ก่อนวันพรีเซนต์

รายการปรับปรุง [index.html](index.html) จัดลำดับความสำคัญสำหรับการนำเดโมไปพรีเซนต์สด
(ทุกข้อใน P0/P1 ผ่านการตรวจสอบกับโค้ดจริงแล้ว · อัปเดตล่าสุด 19 ก.ค. 2026 — รอบ INC-C+G+PIA-001)

ขนาดงาน: `S` = เล็ก (ไม่กี่บรรทัด) · `M` = กลาง · `L` = ใหญ่

---

## P0 — ต้องแก้ก่อนวันพรีเซนต์ (เดโมอาจพังต่อหน้าคนดู)

- [x] ~~ตัดการพึ่งอินเทอร์เน็ต~~ `M` — **เสร็จแล้ว 17 ก.ค. 2026**
  Tailwind, Lucide (ล็อกเวอร์ชัน v1.25.0) และฟอนต์ Noto ทั้ง 24 หน้าตัด **ฝังเข้าไปใน index.html โดยตรง** (ฟอนต์เป็น base64) — ไฟล์เดียวจบจริง ก๊อปปี้ไปเปิดเครื่องไหนก็ได้โดยไม่ต้องมีเน็ตหรือโฟลเดอร์ประกอบ + กัน `lucide.createIcons()` ด้วย `typeof lucide !== "undefined"` — ตรวจแล้วตอนเปิดแอปไม่มี network request ออกอินเทอร์เน็ตเลยแม้แต่ครั้งเดียว

- [x] ~~ปุ่มตายบนหน้าหลัก 2 จุด~~ `S` — **เสร็จแล้ว 16 ก.ค. 2026**
  การ์ด "นัดหมายที่กำลังจะถึง" กดแล้วพาไปหน้าเจาะเลือด และปุ่ม "+ สั่งซื้อยาสามัญเพิ่มเติม" กางรายการยาสามัญ 4 ตัวให้เลือก รวมเข้ายอดชำระ (สมชายที่ไม่มียาประจำก็สั่งได้แล้ว)

- [x] ~~ชื่อซ้ำ "คุณคุณยายบุญมี"~~ `S` — **เสร็จแล้ว 17 ก.ค. 2026**
  template ตัด "คุณ" ที่ติดมากับชื่อออกก่อนเติมคำนำหน้า (`user.name.replace(/^คุณ/, "")`) — สลับเป็น u3 แล้วขึ้น "คุณยายบุญมี สุขใจ" ถูกต้อง

- [x] ~~คำทักทาย + วันนัดเป็นค่าตายตัว~~ `S` — **เสร็จแล้ว 17 ก.ค. 2026**
  ทักทายตามเวลาจริง (เช้า/บ่าย/เย็น จาก `greetingByHour()`) และวันนัดบนการ์ดหน้าแรก render เป็น "พรุ่งนี้" ด้วย `tomorrowThai()` ตรงกับค่าตั้งต้นของหน้าเจาะเลือด (`tomorrowISO()` + 09:00) ที่ทำให้ปุ่มยืนยันนัดหมายกดได้ทันที

- [x] ~~จอโปรเจกเตอร์ตัดขอบล่าง~~ `S` — **เสร็จแล้ว 17 ก.ค. 2026**
  เพิ่ม `fitPhone()` — ย่อเฟรมอัตโนมัติด้วย CSS zoom เมื่อจอเตี้ยกว่าที่เฟรมต้องการ (คำนวณจากขนาดจริง + ฟังทั้งตอนโหลดและตอน resize) — ทดสอบที่ 1366×768 แล้วเห็นครบทั้งเฟรมรวมแถบเมนูล่าง (ย่อเหลือ ~86%)

---

## P1 — ทำแล้วเดโมดูดีขึ้นชัดเจน (เรียงตามผลลัพธ์ต่อแรงที่ลง)

- [x] ~~จ่ายเงินยืนยันคิวแล้วขึ้น "ชำระแล้ว"~~ `S` — **เสร็จแล้ว 17 ก.ค. 2026**
  เพิ่ม `state.queuePaid` — จ่ายแล้วการ์ดเปลี่ยนเป็นเช็คเขียว "ชำระแล้ว · คิวได้รับการยืนยัน" ไม่ทวงเงินซ้ำ และรีเซ็ตเมื่อส่งคำขอปรึกษาใหม่ · **อัปเดต 18 ก.ค.:** ปุ่ม VDO call ล็อกจนกว่าจะชำระเงิน — ตอนยังไม่จ่ายปุ่มเปลี่ยนเป็น "ชำระเงินก่อนเข้าห้อง" กดแล้วพาไปหน้าจ่ายทันที (ไม่มีปุ่มตาย ไม่มีทางตัน)

- [x] ~~หน้า VDO call จำลอง~~ `M` — **เสร็จแล้ว 18 ก.ค. 2026**
  หน้าจอสนทนาเต็มจอโทนเข้ม: เชื่อมต่อ ~2 วิ พร้อมวงหายใจ (ล้อ motif หน้าจิตแพทย์) → ติดสายแล้วตัวนับเวลาเดินจริง, ชื่อ-แผนกหมอตาม `selectedDoctor`, กล้องตัวเอง (PiP) มุมขวา, ปุ่มปิดไมค์/ปิดกล้อง (toggle ได้จริง) และวางสาย → dialog "จบการปรึกษาแล้ว" สรุประยะเวลา + ล้างคิวกลับหน้าแรก · refresh กลางสายแล้วกลับมาต่อได้

- [x] ~~จองแล้วเก็บเข้ารายการนัดหมายบนหน้าแรก~~ `S` — **เสร็จแล้ว 17 ก.ค. 2026**
  ทุก flow ที่จ่ายเงิน (เจาะเลือด/ยา/ไลฟ์สไตล์) push เข้า `state.bookings` แล้วโชว์บน "นัดหมายที่กำลังจะถึง" พร้อมป้ายเช็คเขียว "ยืนยันแล้ว" + ยอดเงิน เรียงใหม่สุดบนสุด — การ์ดชวนจองเดิม (ติดป้าย "ยังไม่ยืนยัน · แตะเพื่อยืนยันนัดหมาย") จะโชว์เฉพาะตอนยังไม่มีนัดจริง
  **ต่อยอดเพิ่ม:** การ์ดนัดหมายกดเข้าไปดูรายละเอียดเต็มได้ (sheet เลื่อนขึ้น: รหัสการจอง OSR-XXXX, ป้ายสถานะ, รายการตรวจ/ยา/ผู้ให้บริการ, สถานที่, ยอดชำระ) พร้อมปุ่มยกเลิกนัดหมาย → คืนเงิน + การ์ดหายจากหน้าแรก

- [x] ~~QR จริงแทนไอคอน~~ `S` — **เสร็จแล้ว 17 ก.ค. 2026**
  ฝัง QR จริง (SVG 33×33 โมดูล สร้างจากข้อความ "ORNSIRIN HEALTH LINK - DEMO PAYMENT") ใช้ทั้งหน้าชำระเงินทุก flow และการ์ดจ่ายหน้าคิว — สแกนได้จริง ถ้ามีคนในห้องลองสแกนจะเจอข้อความเดโม ไม่ใช่ลิงก์เสีย

- [x] ~~หมอตรงกับแผนกที่เลือก~~ `S` — **เสร็จแล้ว 17 ก.ค. 2026**
  เพิ่ม `doctorsBySpecialty` ครบทั้ง 6 แผนก (จิตแพทย์+นักจิตวิทยา / แพทย์ทั่วไป / อายุรแพทย์ / กระดูก / จักษุ / หู คอ จมูก) — เลือกจิตแพทย์แล้วเจอ พญ. พิมพ์ลดา จิตแพทย์ ไม่ใช่แพทย์ทั่วไปอีกต่อไป ชื่อหมอวิ่งตามไปถึงหน้าคิวและ VDO call

- [x] ~~ไอคอนเช็คมองไม่เห็นบนโปรเจกเตอร์~~ `S` — **เสร็จแล้ว 18 ก.ค. 2026**
  เช็คหน้าคิว + เช็ค success dialog เป็นสีขาว, shield-check หน้าแรก + snowflake แท็บ Ice bath เป็นครีมอ่อน #E7EFDD ตามโทนข้อความข้างเคียง — ตรวจ computed color ครบทั้ง 4 จุด

- [x] ~~บริการสัตว์เลี้ยง pc5 คิดเงิน ฿0~~ `S` — **เสร็จแล้ว 18 ก.ค. 2026**
  ให้ราคาแพ็กเกจรายเดือนเริ่มต้นจริง ฿590 — วิ่งผ่าน flow จองปกติครบ รวมส่วนลดลูกบ้าน 15% (590 → 501)

- [x] ~~ตัวควบคุมคิวสำหรับผู้พรีเซนต์~~ `M` — **เสร็จแล้ว 19 ก.ค. 2026 (INC-A โดย Codex, audit PASS, commit `2a8bd34`)**
  แบนเนอร์ "คิวปรึกษาหมอ #N · แตะเพื่อกลับเข้าคิว" บนหน้าแรกเมื่อมีคิวค้าง (กลับเข้าโดยคิว/สถานะจ่ายเงินไม่รีเซ็ต) + คีย์ลัดลับผู้พรีเซนต์: ลูกศรขวา/ซ้าย เร่ง/ถอยคิวในช่วง [0,3] เฉพาะบนหน้าคิวตอนไม่มี overlay — ไม่มี UI บอก คนดูไม่มีทางรู้

- [x] ~~กันดับเบิลคลิกข้ามหน้า QR~~ `S` — **เสร็จแล้ว 18 ก.ค. 2026**
  overlay ที่เพิ่งเปิด (sheet/dialog ทุกชนิด) ไม่รับคลิก 300ms แรกผ่าน `pointer-events:none` — คลิกที่สองของการกดรัวตกไปเฉย ๆ ไม่ทะลุไปโดน "ฉันชำระเงินแล้ว" · ประกอบกับจังหวะตรวจสอบยอด 1.4 วิ ที่กันปุ่มยืนยันซ้ำอยู่แล้ว ปิดช่องข้ามหน้า QR สนิท

- [x] ~~refresh แล้วไม่หลุดกลับจุดเริ่ม~~ `S` — **เสร็จแล้ว 18 ก.ค. 2026**
  state ถูกเก็บลง `sessionStorage` ทุกครั้งที่เปลี่ยน (รวมข้อความอาการที่กำลังพิมพ์) แล้วกู้คืนตอนเปิดหน้า — F5 กลางเดโมกลับมาที่เดิมครบ ล็อกอิน/หน้า/นัด/เลขรันรหัสจอง · ปิดแท็บแล้วเปิดใหม่ = เดโมสดเริ่มต้น (จุดเด่นของ sessionStorage)

---

## P2 — เสริมเสน่ห์ / ไว้หลัง pitch

- [x] ~~กระดิ่งแจ้งเตือน~~ `S` — **เสร็จแล้ว 19 ก.ค. 2026 (INC-E โดย Codex, audit PASS, commit `2a8bd34`)**
  กระดิ่ง + badge บนแถบบน (เฉพาะตอนล็อกอิน) → sheet 3 รายการ (ยาใกล้หมด→หน้ายา · ผลตรวจพร้อม→หน้าผลแล็บ · วัคซีนสัตว์เลี้ยง→Pet care) และแทรก "ใกล้ถึงคิวแล้ว" บนสุดเมื่อมีคิวค้าง (badge 3→4) — แตะแล้วนำทางถูกปลายทาง คิวไม่รีเซ็ต
- [x] ~~หน้าผลตรวจแล็บ~~ `M` — **เสร็จแล้ว 19 ก.ค. 2026 (INC-F โดย Codex, audit PASS, commit `22f413c`)**
  ปุ่ม "ผลตรวจของฉัน" บนหน้าเจาะเลือด → หน้าใหม่แสดงผลจำลองรายสมาชิก (ชิป ปกติ/ควรติดตาม, วันที่คำนวณจากวันจริง, empty state สำหรับคนไม่มีผล) + ปุ่ม "ปรึกษาหมอต่อ" cross-sell
- [x] ~~ซ่อนข้อมูลส่วนตัวก่อนล็อกอิน~~ `M` — **เสร็จแล้ว 19 ก.ค. 2026 (INC-B โดย Codex, audit PASS, commit `2a8bd34`)**
  ก่อนล็อกอิน: หน้าแรกเป็น hero ต้อนรับ generic (ปุ่มบริการ 4 ปุ่มยังโชว์ศักยภาพ) · เข้าหน้าอื่นเจอหน้า "เข้าสู่ระบบก่อนใช้งาน" — ชื่อ/ยา/ผลตรวจ/สัตว์เลี้ยงไม่หลุดก่อนล็อกอินอีกต่อไป
- [x] ~~โหมดผู้สูงอายุของคุณยายบุญมี~~ `M` — **เสร็จแล้ว 19 ก.ค. 2026 (INC-G โดย Codex, audit PASS, commit `ca68ddb`)**
  สลับเป็นคุณยายบุญมี → ทั้งแอป + overlay ขยาย 1.15× อัตโนมัติ (zoom บน `#app`/`#modal-root` compose กับ `fitPhone`) · การ์ด SOS แดงเด่นเฉพาะ u3 บนหน้าแรก แตะแล้ว dialog จำลองทีมดูแลโทรกลับ · สลับกลับ/logout กลับขนาดปกติ
- [x] ~~จังหวะ "กำลังตรวจสอบยอดชำระ..."~~ `S` — **เสร็จแล้ว 18 ก.ค. 2026**
  กด "ฉันชำระเงินแล้ว" → ปุ่มล็อกพร้อมวงหมุน "กำลังตรวจสอบยอดชำระ..." ~1.4 วิ แล้วค่อยขึ้นหน้าสำเร็จ — กันกดรัวซ้ำในตัว และปิดหน้าจ่ายกลางคันจะยกเลิกเงียบ ๆ ไม่เกิดนัดผี
- [x] ~~โปรไฟล์สัตว์เลี้ยง~~ `S` — **เสร็จแล้ว 18 ก.ค. 2026**
  การ์ดโปรไฟล์ผูกกับสมาชิก: สมหญิงมีน้องโบ้ (โกลเด้น ชิปวัคซีนกดจองสัตวแพทย์ได้) · คุณยายมี 2 ตัว — น้องส้ม (แมว วัคซีนครบ) กับน้องข้าวปั้น (ปอม วัคซีนใกล้ครบกำหนด)
  **ต่อยอดเพิ่ม:** ปุ่ม "เพิ่มโปรไฟล์สัตว์เลี้ยง" เปิดฟอร์ม (ประเภทหมา/แมว ชื่อ พันธุ์ อายุ) เพิ่มได้ทุกสมาชิกและรอด refresh · ตอนจองบริการ Pet Care มีตัวเลือก "ใช้บริการสำหรับ" เลือกน้องเป็นรายตัว ชิปวัคซีน preselect ตัวนั้นให้อัตโนมัติ และรายละเอียดนัดระบุชื่อน้อง
- [ ] **หน้าสถิติฝั่งผู้ประกอบการ** `L` — ยอดจอง/บริการยอดนิยม/รายได้ ตอบคำถาม "อรสิรินได้อะไร" — ใหญ่สุดในลิสต์ ใช้สไลด์แทนได้

---

## ลำดับที่แนะนำ

1. ~~ทำ P0 ทั้งหมด~~ — **P0 ครบทุกข้อแล้ว เดโมพร้อมขึ้นเวทีในระดับ "ไม่พังต่อหน้าคนดู"**
2. ~~ตามด้วย P1 ที่เหลือ~~ — **P1 ครบทุกข้อแล้ว 19 ก.ค. 2026** (ปิดท้ายด้วยตัวควบคุมคิวผู้พรีเซนต์)
3. **P2 ที่เหลือ:** โหมดผู้สูงอายุ · หน้าสถิติฝั่งผู้ประกอบการ — เลือกหยิบตามผู้ฟัง (เช่น โหมดผู้สูงอายุ ถ้ากลุ่มเป้าหมายเป็นหมู่บ้านผู้เกษียณ)
